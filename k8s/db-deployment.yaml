apiVersion: apps/v1
kind: Deployment
metadata:
  name: db
  namespace: new-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: db
  template:
    metadata:
      labels:
        app: db
    spec:
      containers:
        - name: db
          # image will be updated by CI to use the commit SHA tag
          image: ravik92/db:1b5f24fd37d4bd432c051962b67e333398a843db
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_USER
              value: "postgres"

---
apiVersion: v1
kind: Service
metadata:
  name: db
  namespace: new-app
spec:
  selector:
    app: db
  ports:
    - port: 5432
      targetPort: 5432
  type: ClusterIP
